# 超シンプル版の実装について

## 概要

このPRでは、T5Gemma-TTSの**超シンプル版**を実装しました。これは、複雑な設定や高度な機能を必要としないユーザーが、最小限の依存関係で素早くTTSを試せるようにするためのものです。

## 追加されたファイル

### 1. `inference_simple.py` (メインスクリプト)
- 最小限のパラメータで音声合成を実行
- コマンドライン引数は3つのみ：
  - `text`: 合成するテキスト（必須）
  - `--model`: モデルのパス（デフォルト: Aratako/T5Gemma-TTS-2b-2b）
  - `--output`: 出力ファイル名（デフォルト: output.wav）
- 高度な機能（voice cloning、duration controlなど）は非対応

### 2. `README_SIMPLE.md` (シンプル版ドキュメント)
- 日本語と英語の両方で記載
- インストール手順
- 基本的な使い方
- 含まれない機能の明確な説明
- 完全版へのリンク

### 3. `requirements_simple.txt` (最小限の依存関係)
- 推論に必要な最小限のパッケージのみ
- 学習用パッケージ（wandb、tensorboardなど）は除外
- Gradio関連も除外

### 4. `examples_simple.py` (使用例スクリプト)
- 英語、日本語、中国語の使用例を実行
- 初心者向けのデモンストレーション

### 5. `SIMPLE_VERSION.md` (比較・説明文書)
- 完全版とシンプル版の比較表
- どちらを選ぶべきかのガイド
- FAQ

## 変更されたファイル

### `README.md` と `README_ja.md`
- シンプル版へのリンクを追加
- ヘッダー部分に目立つように配置

## 使い方

```bash
# 最小限のインストール
pip install -r requirements_simple.txt

# 実行
python inference_simple.py "こんにちは、これは音声合成のテストです。"

# カスタム出力
python inference_simple.py "Hello!" --output my_speech.wav
```

## 設計思想

1. **シンプルさ優先**: 必要最小限の機能のみ
2. **初心者フレンドリー**: 複雑な設定なしで動作
3. **既存コードの活用**: `inference_tts_utils`や`duration_estimator`などの既存モジュールを利用
4. **後方互換性**: 完全版は何も変更せず、新規ファイルとして追加

## 技術的な詳細

- 完全版の`inference_commandline_hf.py`から不要な機能を削除
- シンプルなargparseベースのCLI
- デフォルト値を適切に設定し、ユーザーが指定する必要を最小化
- エラーハンドリングを簡素化

## テスト

- Pythonの構文チェック済み
- 既存のモジュールとの互換性確認済み
- 実際の実行テストは、モデルのダウンロードが必要なため省略

## 今後の改善案

- より詳細な使用例
- 音声品質の比較
- バッチ処理のサポート
- より詳細なエラーメッセージ

## 利点

1. **迅速な試用**: 研究者や開発者がすぐにTTSを試せる
2. **教育目的**: TTS技術を学ぶ学生に最適
3. **デプロイの簡素化**: 推論のみが必要な場合、依存関係を削減
4. **ドキュメントの改善**: 2つのレベルのドキュメントで、様々なユーザーニーズに対応

## 結論

この超シンプル版により、T5Gemma-TTSはより多くのユーザーにアクセスしやすくなり、エントリーバリアが低くなります。同時に、完全版も維持されているため、高度なユーザーは引き続きすべての機能を利用できます。
